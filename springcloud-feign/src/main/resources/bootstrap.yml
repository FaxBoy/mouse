#服务名称
spring:
  application:
    name: feign-service
    #指定从哪个配置中心读取
  cloud:
    config:
      discovery:
        service-id: CONFIG-SERVER
        enabled: true
      #环境
      #profile: dev
      #分支，默认master
      label: master